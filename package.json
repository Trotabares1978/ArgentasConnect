{
  "name": "argentas-exact-android",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "android:prepare": "npx cap add android && npx cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.0",
    "@capacitor/core": "^7.4.0",
    "peerjs": "^1.5.4",
    "com.google.android.gms:play-services-nearby": "19.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.0",
    "typescript": "^5.9.2"
  }
}
